name: Send Weekly Report

on:
  schedule:
    - cron: '0 11 * * 0'  # Runs at 11:00 AM UTC on Sundays
  workflow_dispatch:

jobs:
  send-email:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Run the script
      env:
        EMAIL: mukundan.sankar14@gmail.com
        PASSWORD: CEna14!@
        RECIPIENT: mukundan.sankar14@gmail.com
      run: |
        python send_weekly_report.py
